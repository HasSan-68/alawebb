{% extends 'base.html.twig' %}

{% block title %}Meno{% endblock %}

{% block body %}
    <h1>Meno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meno.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ meno.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ meno.content }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ meno.updatedAt ? meno.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meno_index') }}">back to list</a>

    <a href="{{ path('meno_edit', {'id': meno.id}) }}">edit</a>

    {{ include('meno/_delete_form.html.twig') }}
{% endblock %}
